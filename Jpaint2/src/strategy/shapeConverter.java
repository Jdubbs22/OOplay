package strategy;
import java.awt.Graphics2D;
import java.awt.Shape;
import  java.awt.event.MouseAdapter;
import java.awt.event.MouseEvent;
import java.awt.geom.Ellipse2D;

import model.ApplicationState;

public class shapeConverter{
	IStratMousePaintController strategy;
	
	public void ShapeConverter() { 
	 
	/*	if(applicationState.(ELLIPSE)) strategy = new EllipseStrategy(); 
		else if(applicationState.setActiveShape(RECTANGLE)) strategy = new RectangleStrategy(); 
		else (applicationState.setActiveShape(TRIANGLE))strategy = new TriangleStrategy(); 
*/
	
	
	}// end ShapeConverter
	
	
	
	
}//end shapeConverter